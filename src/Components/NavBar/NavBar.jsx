import './NavBar.css'

const Header = (props) => {

    return (
        <h3 className='header-style'>Social
            <span className='text-muted text-update-header'>Feed</span></h3>
    );
}

export default Header;